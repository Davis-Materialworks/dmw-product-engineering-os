# Workflows

Root-level workflow documentation points to the agent and platform workflows.

Use:

- `adapters/` for host-specific workflows.
- `compatibility/routing-matrix.md` for task routing.
- `scripts/` for validation, scoring, and benchmark automation.
- `knowledge/evolution/` for post-project learning workflows.
