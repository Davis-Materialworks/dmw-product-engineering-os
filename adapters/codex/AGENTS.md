# Codex Adapter

Role: IMPLEMENTATION ENGINE.

Use Codex for repository-wide changes, large refactors, bulk generation, benchmark automation, scripts, validation, and turning approved architecture into code.

Always read the core Constitution and routing matrix before work. Prefer small reviewable diffs and run available checks.
